jobs:
########################## NATIVE WAR CI/CD JOBS #########################
# manifest that defines the Native WAR service definition
  - name: demo-war-manifest
    type: manifest
    steps:
      - IN: demo-war

# deploy job that deploys native war to cluster of VMs
  - name: demo-war-test-deploy
    type: deploy
    steps:
      - IN: demo-war-manifest
        force: true
      - IN: demo-war-test-cls
      - TASK:
        - script: sudo rm -rf /usr/local/tomcat/webapps/HelloWorld.war
        - script: sudo cp /tmp/shippable/demo-war-manifest/demo-war/HelloWorld.war /usr/local/tomcat/webapps/HelloWorld.war
